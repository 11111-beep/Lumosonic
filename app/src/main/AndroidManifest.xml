<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- 允许应用访问互联网 -->
    <uses-permission android:name="android.permission.INTERNET" /> <!-- 允许应用使用麦克风录制音频 -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" /> <!-- 允许应用读取手机状态 -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" /> <!-- 允许应用读取外部存储（如SD卡） -->
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" /> <!-- 允许应用写入外部存储（如SD卡） -->
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" /> <!-- 允许应用读取媒体图像 -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" /> <!-- 允许应用读取媒体视频 -->
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" /> <!-- 允许应用读取媒体音频 -->
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" /> <!-- 允许应用发送通知 -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" /> <!-- 允许服务在前台运行 -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" /> <!-- 允许服务在前台运行 -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" /> <!-- 前台服务的具体用途是媒体播放 -->
    <uses-permission android:name="android.permission.WAKE_LOCK" /> <!-- 允许应用保持设备唤醒 -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" /> <!-- 允许应用忽略电池优化 -->
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" /> <!-- 允许应用访问网络状态 -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" /> <!-- 允许应用访问WiFi状态 -->

    <application
        android:name=".App"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.Lumosonic"
        tools:targetApi="31">
        <activity
            android:name=".ui.home.RecentMusicActivity"
            android:exported="false" />
        <activity
            android:name=".ui.user.FavoriteMusicActivity"
            android:exported="false" />
        <activity
            android:name=".ui.user.LocalMusicActivity"
            android:exported="false" />
        <activity
            android:name=".intent.SearchActivity"
            android:exported="false" />
        <activity
            android:name=".ipc.MusicActivity"
            android:exported="false" />

        <receiver
            android:name="androidx.media.session.MediaButtonReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MEDIA_BUTTON" />
            </intent-filter>
        </receiver>

        <service
            android:name=".ipc.MusicService"
            android:enabled="true"
            android:exported="true"
            android:foregroundServiceType="mediaPlayback"
            android:process=":remote">
            <intent-filter>
                <action android:name="android.intent.action.MEDIA_BUTTON" />
            </intent-filter>
        </service>

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>